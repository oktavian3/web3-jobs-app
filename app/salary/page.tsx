import roles from '@/public/data/roles.json';
const selected=(roles as Array<{name:string;category:string;avgCompRange:{usd:string;note:string}}>).slice(0,8);
export default function SalaryPage(){return <main className="min-h-screen bg-[#f6f8fc] px-4 pb-24 pt-36"><div className="mx-auto max-w-6xl"><div className="max-w-3xl"><span className="section-kicker">Salary guide</span><h1 className="mt-4 text-4xl font-extrabold tracking-[-.05em] md:text-6xl">Know the range before you negotiate.</h1><p className="mt-5 leading-7 text-slate-600">These are broad global remote compensation ranges from the role dataset. Token grants can be valuable, volatile, or worthless—evaluate them separately from cash.</p></div><div className="mt-12 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="grid grid-cols-[1fr_.9fr_.75fr] bg-slate-950 px-5 py-4 text-xs font-black uppercase tracking-widest text-slate-300"><span>Role</span><span>Global cash range</span><span>Category</span></div>{selected.map((role)=><div className="grid grid-cols-[1fr_.9fr_.75fr] items-center border-t border-slate-100 px-5 py-5 text-sm" key={role.name}><strong className="text-slate-950">{role.name}</strong><span className="font-bold text-blue-700">{role.avgCompRange.usd}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-center text-xs font-bold capitalize text-slate-600">{role.category}</span></div>)}</div><div className="mt-8 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm leading-6 text-amber-900"><strong>Context note:</strong> Global remote ranges can differ significantly from local Indonesian market offers. Compare cash, vesting schedule, token liquidity, contract type, benefits, and runway before deciding.</div></div></main>}
